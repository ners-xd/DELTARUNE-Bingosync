/// PATCH

/// AFTER
    if (timer == 1)
        snd_play(snd_cymbal_reverse);
    
    if (timer == 10)
    {
/// CODE
        scr_add_goal_progress(143, 1);
        scr_add_goal_array(110, "pink");
        scr_add_goal_array(135, "pink");
/// END